$kind: http-request
description: Approves a payroll record for payment.
url: "{{baseUrl}}/api/v2/payroll/records/:id/approve"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  id: ""
body:
  type: text
  content: |-
    {
      "approvedBy": "manager_123",
      "notes": "Approved for payment"
    }
order: 5000
